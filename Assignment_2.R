Name <- c("Jeb", "Donald", "Ted", "Marco", "Carly", "Hillary", "Berine")

ABC_results <- c(4, 62, 51, 21, 2, 14, 15)

CBS_results <- c(12, 75, 43, 19, 1, 21, 19) 

df <- data.frame(Name, ABC_results, CBS_results)
df

#in the 2016 election (not actual results), we can observe with the dataframe that Donald had majority vote.